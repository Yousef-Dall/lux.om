# Production API origin, without a trailing slash.
# Leave empty only when frontend and API share the same origin.
VITE_API_URL=""

# Development-only Vite proxy target.
VITE_API_PROXY_TARGET="http://localhost:4000"
